export class Interface {
    constructor(x, y) {
        this.x = x;
        this.y = y;
        this.cellType = "interface";
        this.segments = [];
        this.switches = [];
    }
    initSegments(grid) {
    }
    initSwitches(grid) {
    }
    draw(context) {
    }
}
